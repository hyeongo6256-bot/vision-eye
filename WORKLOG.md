# 작업 로그

vision eye 리뉴얼 프로젝트(`visioneye-renewal/`) 작업 기록입니다. 프로젝트 코드는 이 폴더 한 곳에서만 관리하고, 날짜별 작업 내용은 이 파일에 섹션으로 누적합니다.

## 2026-09-09

- 기존 `visioneye.co.kr/` 정적 스냅샷을 참고해 Vite + React 리뉴얼 프로젝트(`visioneye-renewal/`) 최초 생성
- Header, Footer, HeroSlider, SpecialtyShowcase, TreatmentHighlights, VisionTv, ClinicTabs 컴포넌트 구현
- 히어로 섹션 디자인 다수 반복 (SUIT 폰트 적용, 텍스트 정렬/자간, 로고 아이콘+워드마크 분리)
- 헤더 스크롤 시 투명 → 흰 배경 전환 구현
- 저장소 git 초기화 후 GitHub(`hyeongo6256-bot/vision-eye`)에 최초 커밋 푸시 (SSH 인증)

## 2026-09-14

- SpecialtyShowcase 커버플로우 카드 크기/간격 조정, 비활성 카드 블러 처리
- ClinicTabs를 "our CLINIC" 번호 목록 + 원형 사진 + 고정 인용구 레이아웃으로 재구성, 클릭 대신 호버로 전환되도록 변경
- 진료분야(TreatmentHighlights) 벤토 그리드 디자인, "다이얼" 형태 디자인을 각각 시도했으나 최종적으로 원래 레이아웃으로 되돌림
- 변경사항 커밋 및 푸시

## 2026-09-15

- 홈페이지 섹션 순서 변경: 히어로 → SPECIALITY → our CLINIC → 진료분야 → 비전 TV(마지막)
- SPECIALITY 제목 폰트를 our CLINIC과 동일하게 맞추고 따옴표 제거
- 신규 "Blue Eye Center" 섹션 추가: 남색 배경 고정(패럴랙스) + 안경 이미지 히어로 + 진료분야 5개 항목이 좌우 번갈아 배치되는 스크롤 목록
- **작업 방식 정리**: 매일 프로젝트 전체를 날짜 폴더로 복사하던 방식을 중단하고, 프로젝트를 저장소 최상단 `visioneye-renewal/` 한 곳으로 통합. 이후 날짜 폴더는 만들지 않고, 이 `WORKLOG.md`에 날짜별로 작업 내용만 기록함.
- Blue Eye Center 스크롤 방식을 여러 차례 시도(연속 이동 애니메이션 → 정적 나열 → sticky 스택) 끝에, 화면 한 자리(sticky stage)에서 안경 배너/진료분야 1~5번 내용이 순서대로 교체되는 방식으로 확정
- 레거시 `common.css`의 `body { overflow: hidden }`이 `position: sticky`를 무력화시키는 원인이었음을 확인하고 `src/index.css`에서 `overflow: visible`로 되돌려 수정
- 진료분야 1~5번 카드는 opacity 페이드로 전환되고, 안경 배너는 페이드 없이 스크롤량에 비례해 위로 이동하며 사라지도록 구현
- 진료분야 카드의 사진/텍스트/폰트 크기를 소폭 확대
- `react-router-dom` 도입, 서브페이지 라우팅 구조 추가 (`/system`, `/doctor`, `/contact`)
- 서브페이지 공통 히어로 배너(`page-hero`) 컴포넌트 스타일 추가 — 서브페이지에서는 헤더가 항상 흰 배경으로 고정되도록 `Header`에 라우트 인식 로직 추가
- "의료진 소개"(`/doctor`) 페이지 구현: 병원명/원형 사진/인용구 + 전문의 약력·학회 목록 2단 레이아웃, 원본 사이트의 서브페이지 공통 하단 배너(`/img/page/bottom-common.jpg`) 내용을 텍스트로 재구성해 추가
- "오시는 길"(`/contact`) 페이지 구현: 진료안내/연락처/주소 + 원본 사이트의 구글 지도 embed 연결
- 하단 4단 배너(`doctor-statement`, `doctor-promo`)를 `sitePromo.css`로 분리해 여러 서브페이지에서 재사용 가능하도록 정리

## 2026-09-16

- 세로 스크롤바 유무에 따라 콘텐츠 폭이 흔들리던 문제를 `html { overflow-y: scroll }`로 고정해 해결 (페이지 이동 시 레이아웃이 좌우로 흔들리던 원인)
- "오시는 길" 페이지 레이아웃을 참고 이미지에 맞춰 재구성: 좌측 로고(우측 정렬)+우측 진료안내/상담문의/오시는길(남색 배지) 2단 레이아웃, 두 블록을 서로 수직 중앙 정렬
- 지도 위 프레임 디자인을 시도했다가("짜치다"는 피드백으로) 되돌림 — 지도 자체 기능은 그대로 유지
- "의료진 소개" 하단 4단 배너: 원본 사이트의 공통 배너(`/img/page/bottom-common.jpg`)에서 실제 사진을 크롭해 각 카드 전체 배경으로 사용하도록 변경, 이후 라운드 처리 + 카드 사이 간격 추가, 배경색 제거
- "평택비전안과의원은" 제목을 4단 배너 위, 구분선과 이미지 사이 정중앙(위아래 55px)에 배치, 브랜드 블루 색상 적용
- "평택비전안과의원은 ..." 문구를 4단 배너 아래(푸터 위)로 이동
- 원장님 프로필 원형 사진: 눈/코 중심 좌표를 픽셀 단위로 측정해 정확히 가운데로 재크롭 (이후 사용자가 직접 크롭한 사진으로 최종 교체)
- "비전안과 소개"(`/about`) 페이지 신규 추가: 원본 사이트 본문에 있던 라운드 사진을 크롭해 라운드 없이 풀스크린 히어로로 사용, 헤더는 (홈페이지와 달리) 흰 배경으로 고정
- 검증 방식 도입: 헤드리스 Chrome(`--headless --screenshot`)으로 직접 렌더링해서 스크린샷으로 확인 후 보고하는 절차를 적용 (구글 지도 타일은 헤드리스 환경 한계로 안 보임 — 프레임 등 나머지 요소는 정상 검증됨)

## 2026-09-17

- "비전안과 소개"(`/about`) 히어로 사진을 사용자가 제공한 실제 사진(의사+환자+모니터)으로 교체
- 히어로 문구를 레퍼런스대로 재구성: "VISION EYE CENTER" 큰 글씨 + "안녕하세요. 평택 비전안과의원입니다." + 영문 서브텍스트, 위치/크기를 여러 차례 조정(오른쪽 정렬 → 펜으로 가리키는 모니터 쪽으로 이동, 폰트 크기 확대, 색상 흰색으로 통일)
- 히어로 아래 소개 문구 섹션 추가: 원본 사이트(about.html) 본문 텍스트를 그대로 옮김 (경력 소개 문단 + 약속 문단 + 서명), 서명은 본문과 같은 왼쪽 정렬로 수정
- 하단 원형 다이어그램 섹션 추가: 배경 사진 위에 원 5개(모서리 4개 + 중앙 1개)와 연결선을 CSS로 직접 구현(원본 이미지에 원이 그려진 버전 대신, 나중에 수정하기 쉽도록 사진만 배경으로 쓰고 원/선/텍스트는 코드로 그리는 방식 선택)
- 검증 도구 보강: 헤드리스 스크린샷이 뷰포트 높이만큼만 찍혀 페이지 하단이 안 보이는 문제를 발견 → `puppeteer-core`를 임시 설치해 실제 브라우저에 원격 연결(`--remote-debugging-port`)한 뒤 `fullPage` 스크린샷으로 페이지 전체를 확인하는 방식으로 개선

## 2026-09-20

- "진료시스템"(`/system`) 페이지 신규 구현: 히어로 사진 교체 + "VISION EYE CENTER"(오른쪽 정렬) / "진료시스템 SYSTEM" 좌우 배치, 두 블록의 상/하단을 각각 정렬해 정돈된 느낌으로 조정
- 원본 사이트 실제 렌더링을 헤드리스 브라우저로 직접 열어 장비 목록(4열×3행, 12종)이 하나의 이미지(sub_02.png)에 통째로 박혀있는 것을 확인 → 각 장비 사진을 개별 PNG 12장으로 크롭하고 이름/모델명은 실제 텍스트로 새로 작성, 3열×4행 그리드로 재배치
- 장비 사진 중 배경(그라데이션/검정)이 있는 3장은 자동 배경제거(플러드필, rembg 등)를 시도했으나 결과가 부자연스러워 원본 그대로 유지하기로 결정
- 히어로 배경을 사용자가 제공한 새 사진으로 교체하고 `background-attachment: fixed`로 고정, 스크롤 시 아래 콘텐츠가 그 위로 지나가는 패럴랙스 효과 적용 (장비 그리드까지 확장했다가 다시 히어로만 적용하는 것으로 되돌림)
- 원본 사이트의 장비 상세 브로슈어(LightMed YAG Laser, Dry Eye Scan) 내용을 우리 사이트 톤(남색 카드 + 골드 포인트)에 맞게 재구성해서 장비 그리드 아래에 추가
- 진료시스템 페이지에서만 공통 Footer를 숨기도록 처리 (다른 페이지는 그대로 유지)
